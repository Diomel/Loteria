<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.lot.loteria.loteria.LoginActivity">

    <EditText
        android:layout_width="384dp"
        android:layout_height="wrap_content"
        android:inputType="textPersonName"
        android:ems="10"
        android:id="@+id/etUsername"
        android:hint="Username"
        app:layout_constraintLeft_toLeftOf="@+id/activity_login"
        app:layout_constraintTop_toTopOf="@+id/activity_login" />

    <EditText
        android:layout_width="384dp"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/etPassword"
        app:layout_constraintLeft_toLeftOf="@+id/activity_login"
        app:layout_constraintTop_toBottomOf="@+id/etUsername"
        android:hint="Password" />

    <Button
        android:text="Login"
        android:layout_width="384dp"
        android:layout_height="wrap_content"
        android:id="@+id/bLoging"
        app:layout_constraintLeft_toLeftOf="@+id/activity_login"
        app:layout_constraintTop_toBottomOf="@+id/etPassword" />

    <TextView
        android:text="Register Here"
        android:id="@+id/tvRegisterHere"
        android:layout_width="100dp"
        android:layout_height="20dp"
        android:textAlignment="center"
        app:layout_constraintLeft_toLeftOf="@+id/bLoging"
        app:layout_constraintTop_toBottomOf="@+id/bLoging"
        android:layout_marginTop="16dp"
        app:layout_constraintRight_toRightOf="@+id/bLoging" />
</android.support.constraint.ConstraintLayout>
